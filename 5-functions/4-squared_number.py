# Write a function that accepts a single argument, 
# a number, and returns the result of multiplying 
# its argument by an exponent of 2 (also known as 
# squaring the number or raising the number to the 
# power of 2).

# Copy Code
# squared_number(3)   # 9


def squared_number(number):
  return number * number # or return number ** 2

print(squared_number(3))