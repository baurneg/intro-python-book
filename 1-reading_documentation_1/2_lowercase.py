# Determine whether Python has a method to lowercase a string, 
# for example converting 'Aloha, World!' into 'aloha, world!.

Located in built-in type functions for string. 
str.lower() - returns a copy of the string in lower case

Strings are immutable therefore a copy of the string is returned. 