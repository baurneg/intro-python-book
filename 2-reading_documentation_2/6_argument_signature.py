# How many arguments does the str.join method expect? 
# What happens if you call it with fewer or more arguments?

# Str.join method expects one iterable argument with a separator 
# called before the method is invoked. 

name = ['bob', 'kim', 'dong']
name2 = 'sam'
my_str = ''

print(' x '.join(name))